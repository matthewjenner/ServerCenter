// ServerCenter-managed CS2 gameserver.cfg for {{instance.name}}. Rendered per instance from the
// server-instance params; edits made here are overwritten on the next config-apply.
hostname "{{hostname}}"
rcon_password "{{rcon.password}}"
sv_setsteamaccount {{gslt}}
sv_lan {{sv.lan}}
